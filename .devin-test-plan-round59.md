# Round 59 商品草稿模块走查测试计划 (exploratory audit)

环境：docker compose full 栈已运行。Admin http://127.0.0.1:8000，Backend http://127.0.0.1:8080。
账号：admin@example.com/admin123456（主走查）；demo_readonly / demo_operator 用于角色核查。
性质：探索性走查，发现 P0/P1/P2 全部记录，不修复代码。删除/归档只用自建商品。
全程录屏 + annotate（浏览器部分）；curl 部分只做文本证据。全程监控 console error 与 4xx/5xx。

## T1 列表页 /product/drafts（admin）
1. 打开列表：断言 20 条 demo 草稿加载，运营进度列显示 Progress+step Tag（operationProgress 摘要）；无 console error。
2. operationStep 筛选：选「待优化标题」查询 → 断言 URL query 带 operationStep=title 且返回行的运营进度 Tag 与筛选一致（抽查首行）；再选「可以生成刊登草稿」核查。
3. 关键字筛选：输入某已知商品标题片段 → 断言只返回匹配行；输入乱串 → 断言出现「没有匹配的商品草稿」EmptyState 且「重置筛选」可恢复全量。
4. 分页：pageSize 切 10 → 断言 2 页、翻页 URL page=2、数据变化。
5. 批量选择：勾选 2 行 → 断言出现 selection toolbar（已选择 2 个商品）与批量按钮组。注意：任务提到「导出上架清单」按钮，代码中不存在——核实 UI 实际按钮集合并记录差异（预期按钮：批量 AI 优化 / 批量 AI 图片处理 / 批量发布检查 / 批量创建刊登草稿 / 批量设置发布价）。
6. 批量发布检查 Drawer：打开 → operator/店铺列表行为、无授权店铺时的提示是否可行动。

## T2 详情页 /product/drafts/:id（admin）
1. 从列表点入详情 → 断言基本信息 Tab 加载、来源链接 sourceUrl 展示且可点击（新窗口）；采集任务回链是否存在并可回跳（若无，记录）。
2. 基本信息编辑：修改标题后保存 → 断言 message 成功 + 返回列表标题已更新（PUT /api/v1/products/:id 200）。
3. SKU：新增一个 SKU（名称/价格/库存）→ 保存成功并出现在表格；编辑库存；删除该 SKU（Popconfirm）。
4. 图片：上传一张本地图片 → 断言出现在图片列表；排序/删除；「同步外链图片」入口行为（未配置时提示是否清晰）。
5. AI 标题优化：点击生成 → 当前无 AI Provider，断言降级提示清晰可行动（应指向设置页，而非裸报错/500）；应用/撤销按钮在无结果时应禁用或有清晰状态。AI 描述生成同理。

## T3 归档/删除（用自建商品）
1. 新建草稿（列表页「新建草稿」ModalForm）→ 创建成功进入列表。
2. 详情页「删除草稿」：断言 Popconfirm 二次确认出现；确认后 DELETE /api/v1/products/:id 200，返回列表该商品不再出现（列表刷新）。
3. 归档按钮：对另一自建商品执行归档，断言状态变化与提示。

## T4 三角色核查
1. readonly 登录 UI：/product/drafts 列表「新建草稿」「更多」等写入口应隐藏/禁用；勾选行为与批量按钮；详情页编辑/保存/删除/应用 AI 应只读（只读模式 Tag）。任何可点的写按钮点了应被拦截。
2. readonly API（curl，readonly token）：POST /api/v1/products、PUT /api/v1/products/:id、DELETE /api/v1/products/:id → 断言 HTTP 403（非 200/500）。
3. operator 登录（无店铺 authorizedShopId 空）：列表行为记录——预期 scope 过滤后空列表或明确提示；记录实际行为。
4. 越权/不存在：admin token GET /api/v1/products/00000000-0000-0000-0000-000000000000 → 断言 404 且响应不泄露存在性差异。

## T5 响应式（admin，列表+详情）
- 375×812 / 768×900 / 1440×900：断言根节点无横向溢出（document.documentElement.scrollWidth <= innerWidth+4）、表格可横向滚动、Tabs/Modal 可用、selection toolbar 不破版。截图各档。

## 通篇
- 记录所有 console error、网络 4xx/5xx（非预期的）。按 P0/P1/P2 归入 test-report.md，附复现步骤与截图。
