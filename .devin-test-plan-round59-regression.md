# Round 59 修复后回归走查计划

环境：docker compose full 栈已重建（backend/admin 均含修复代码）。Admin http://127.0.0.1:8000。
代码依据：admin/src/pages/Product/Drafts/index.tsx L552/580-587/601/630（readonly 隐藏 + operationStep Alert）；
admin/src/pages/Product/DraftDetail/index.tsx L2603-2615（归档 Popconfirm）；admin/src/services/request.ts L54-60（错误规范化）。

## R1 admin：AI 未配置降级文案（原 P1-1）
1. admin 登录 → 草稿详情（R1 demo multi SKU product）→ AI 文案 Tab → 生成 AI 标题 → 提交。
   - PASS：toast/错误显示含「请配置 base_url」的可行动中文文案。
   - FAIL：仍显示 `Request failed with status code 400`。

## R2 admin：归档二次确认（原 P2-1）+ 删除回归
1. 列表新建一次性草稿「R59 regression fixture」→ 详情页点「归档」。
   - PASS：出现 Popconfirm「确定归档该草稿？」，确认后 toast 已归档、状态 archived。
2. 删除该 fixture（Popconfirm → 确认）。
   - PASS：删除后列表恢复 26 条，fixture 不在列表。

## R3 admin：新建 SKU 后行操作完整（原 P2-5）
1. 在 fixture（归档前）SKU Tab 新建 SKU R59-REG/1.00/1 → 保存。
   - PASS：不刷新页面，新行立即同时显示「编辑」「删除」。
2. 删除该 SKU（清理）。

## R4 admin：operationStep 筛选说明 Alert（原 P2-3 缓解）
1. 列表展开筛选 → 运营进度选「待生成描述」→ 查询。
   - PASS：列表上方出现 info Alert「运营进度筛选展示「该步骤尚未完成」的商品；行内标签是每个商品当前所处步骤，可能早于所筛步骤。」
   - 清除筛选后 Alert 消失。

## R5 admin：批量发布检查无授权店铺空态（原 P2-4）
1. 勾选 2 行 → 批量发布检查。
   - PASS：抽屉显示警告空态（无授权店铺提示），店铺选择禁用。

## R6 readonly：UI 写入口隐藏（原 P1-2）+ API 403
1. demo_readonly 登录 → /product/drafts。
   - PASS：无「新建草稿」「更多」按钮。
2. （列表为空 scope，批量工具栏无法触发——记录）直接访问 admin 可见草稿详情 URL 会 404，改为验证详情按钮由代码 readonly 分支控制 + curl。
3. curl（readonly token）：
   - PUT /api/v1/products/:id → 403 + 「当前账号为只读权限，无法执行此操作」
   - POST /api/v1/products/:id/skus → 403 同文案
   - POST /api/v1/products/:id/ai/optimize-title → 403 同文案
   - FAIL：任何一个返回 404/200 或其他文案。

## R7 响应式快查
- CDP 模拟 375×812 / 768×900 / 1440×900：列表与详情 `scrollWidth <= innerWidth+4`。

## 收尾
- 全程录屏+注解；更新仓库根 test-report.md（覆盖为修复后回归结果）；清理 fixture。
