# UX 视觉复核 v8 报告（重点：v7 历史 P2 收口复核 + R124–R128 新面走查）

- 复核日期：2026-08-06
- 复核角色：user-experience-officer（协同 ui-designer 视角，真实卖家走查，Docker 全栈 demo 环境实测）
- 基线：main `16f1ec52`（R124–R128 之 #264/#266/#267/#268/#269/#270/#271 均已合并）+ 本地叠加唯一未合并 PR #272（R128 线1：seed 第二租户 + 自动化操作日志中文化），合并冲突仅 `docs/PROGRESS.md`（双侧 Round 条目并存解决）
- 环境：`docker compose -f docker-compose.full.yml up -d --build` + `DB_HOST=127.0.0.1 pnpm seed:demo:full`
- 视口：375 / 767 / 768 / 769 / 1440 五档（含 768 断点双侧边界）；三角色（demo_admin 全量、demo_operator 实走、demo_tenant2_admin 隔离抽查）
- 硬指标：走查页面 console error = 0、pageerror = 0、根节点横向溢出 = 0（`document.documentElement.scrollWidth <= clientWidth` 全 true）、403/500 请求噪音 = 0

## 一、走查范围

### v7 历史 P2 逐项复核

| v7 编号 | 页面 | 问题 | v8 结果 |
| --- | --- | --- | --- |
| P2-1 | /orders/automation-logs | 375/768 结果长文本行高极端拉伸 | **收口**。375/768 下数据行高一致（69px），长结果单行 ellipsis |
| P2-2 | /customer/buyer-messages | 保存草稿后订单列瞬时重复/截断渲染 | **收口**。两次编辑保存订单列稳定无闪烁 |
| P2-3 | 选品任务详情 | 768 视口摘要「Shopee / SG」竖排折行 | **收口**。摘要项单行显示 |
| P2-4 | 采购入口 | /purchase/orders 深链 404 | **收口**。已重定向到 /procurement/orders（#264） |

结论：v7 P2 四项全部收口，无回退。

### 重点新面（R124–R128 交付）

| 面 | 路由 | 结果 |
| --- | --- | --- |
| 自动化规则（含 R126 新动作参数表单） | /settings/order-automation-rules | 通过 |
| 自动化执行日志（筛选/失败重试/真实触发） | /orders/automation-logs、订单详情 `?tab=automation` | 通过 |
| 买家消息工作台（待发消息 + 节点规则 tab） | /customer/buyer-messages | 通过 |
| 财务对账三页（对账差异/回款与费用/对账报表） | /finance/* | 通过 |
| 选品数据面（数据面板/价格走势/对比/CSV 导出） | /selection/tasks/&lt;uuid&gt; | 通过 |
| 768 断点后移动模式全动线（#264 断点互斥） | 375 底部导航五 tab + 767/768/769 边界 | 通过 |

## 二、走查评价

### 1. 自动化规则与执行日志

- 规则 CRUD/启停/dry-run 顺畅；R126 新动作 `apply_shipping_rule` 表单出现「应用方式」（仅推荐物流商 / 直接应用物流商）、`assign_warehouse` 出现「分仓策略」，表格参数列中文标签正确。
- dry-run 输出全中文，含安全边界解释（审单待审/挂起不自动化）。
- 执行日志筛选正常；DEMO-AT-1201（发货规则成功）/DEMO-AT-1202（分仓库存不足失败）样本可见；失败重试尝试次数 3→6，中文失败原因，无 5xx。
- 真实触发：DEMO-AT-1004 批量「标记已付款」→ 新增 3 条成功记录（生成采购单/应用发货规则/自动分仓），订单详情 `?tab=automation` 中文轨迹完整。
- #272 中文化验证通过：操作日志 create/update/delete/execute/retry 可见 message 全中文，无 `name= event= action=` 或英文枚举残留（操作类型标识列见 P2-B）。

### 2. 买家消息工作台

- 待发消息筛选/编辑/保存闭环稳定；节点规则 tab 中文渲染、启停/编辑/删除入口齐全；「不会自动发送到平台」降级说明清晰。

### 3. 财务对账

- 对账差异（指标卡 + 语义 Tag：少款/多款/未回款/已结清/差异较大）、回款与费用（双 tab、登记回款、CSV 批量导入）、对账报表（店铺×月份汇总，「未折算」口径提示不伪造汇率）均通过，无英文残留。

### 4. 选品数据面

- 数据面板抽屉（价格走势图 chartTokens 配色一致 + AI 评分明细）、多选「对比所选」抽屉、CSV 导出成功（UTF-8 BOM + 中文表头，内容核对无误）。

### 5. 响应式与多角色

- 375 底部导航五 tab（首页/订单/采购/库存/我的）逐一走通，触屏目标 75×56px（≥40px）。
- 767 = 移动布局、768/769 = 桌面侧栏布局，断点互斥（#264）切换正确，三宽度均无根节点溢出。
- operator：数据按店铺范围隔离（仅见 DEMO-手工渠道店），无 403/500 噪音。
- tenant2（#272 新 seed）：仅见 DEMO-T2-* 订单与规则，租户 1 数据零泄漏。

## 三、问题清单

### P0 / P1

无。

### P2（排序清单，不在本轮修复）

| 编号 | 页面 | 问题 | 建议 |
| --- | --- | --- | --- |
| P2-A | /orders/automation-logs | 第二租户与 operator 视角下为空态：seed 未给租户 2 生成执行记录，operator 店铺范围（手工渠道店）看不到抖店样本。渲染正常、属预期数据/范围行为，但第二租户无法演示 #272 的日志中文化 | 给 DEMO-T2 补 1–2 条执行日志 seed（成功+失败各一） |
| P2-B | /system/operation-logs | 操作类型标识列仍显示英文技术 key（如 `order_automation · execute`）；可见 message/资源/状态均已中文化 | 为操作类型列增加中文映射，实现界面英文残留严格为零 |

## 四、回归结论

- v7 历史 P2 四项全部收口，无回退；未发现 P0/P1。
- 硬指标全零：console error=0、pageerror=0、根节点横向溢出=0、403/500 噪音=0。
- **#272（R128 线1）功能与 UX 验证通过：第二租户隔离正确、自动化日志中文化生效，可合并（需先解 `docs/PROGRESS.md` 冲突）。**
- 覆盖边界说明：财务报表页当前 seed 下以表格+费用构成气泡呈现，未出现大图表，chartTokens 一致性仅在选品价格走势图上验证；readonly 角色本轮未独立实走（admin+operator+tenant2 实走）。
- 本轮为纯走查归档，无代码变更，无受影响门禁；录屏与详细走查报告（含截图）作会话附件不入库。
