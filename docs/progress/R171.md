# R171 线1：全站大回归 v30

- 日期：2026-08-08
- 角色：qa-engineer（company-os）
- 分支：`integration/r171-regression-v30`（基于最新 main `1c9c2b5a`）
- 口径：本地全套门禁 + Docker 全栈实测；Actions CI 不作依据；证据外置作 PR 附件不入库

## 结论

**通过，0 P0 / 0 P1。** 任务下发的 PR 积压（#335/#337/#338/#339/#340/#341/#325/#327/#324/#328/#329/#336）经权威 PR 状态逐个核实**已全部合入 main**；当前唯一功能性 OPEN PR 为 **#342**（R170 线1），与 main **无冲突**（fast-forward，0 文件冲突）。main + #342 叠加后全套门禁全绿（Go 103 包、permmatrix 30 探针 sweep + viewOnlyOperator 契约行、securitytests 106 用例、前端 359、契约 17、collector 18、全量 E2E 359 passed/3 skipped/0 failed），Docker 重建 backend/admin 镜像全栈实测 19/19 断言 PASS，双租户 clean/verify 零残留。

## 一、PR 状态核实（权威 PR 状态，非文档口径）

| PR | 状态 | 说明 |
| --- | --- | --- |
| #324 #325 #327 #328 #329 | merged | 早前轮次已合入 |
| #332（view-only 全站扫尾）/#333 | merged | R166 集成面已在 main |
| #335（审单整批 403 定案，含 #332/#333 内容） | merged | 任务背景中标注「审单语义+P2」——实际已合入 |
| #336 #337（40303 文案统一）#338 | merged | #337 已合入 main **核实属实** |
| #339 #340 #341 | merged | R169 线2 / UX v11 / 竞品 v9 报告 |
| **#342** | **OPEN** | 唯一待合功能 PR（R170 线1：ratelimit 200ms 上界 + mcp-tokens 文档链接 + 验收包增量） |
| #245 #247 #248 | OPEN（冗余） | 三分支 head 均已是 main 的 ancestor（`git merge-base --is-ancestor` 验证），内容已全部在 main，建议直接关闭 |

## 二、集成与冲突

`integration/r171-regression-v30` = main `1c9c2b5a` + `origin/devin/1786148807-r170-line1`（#342）。**fast-forward 合入，零冲突**（#342 分支已自带 main 合并）。无 PROGRESS/验收表 keep-both 需要。

**合并顺序结论：直接合并 #342 即可（无依赖、无冲突）；随后关闭冗余 #245/#247/#248。**

## 三、门禁结果矩阵（本地实跑）

| 门禁 | 结果 |
| --- | --- |
| backend `gofmt -l`（空）/ `go vet` / `go test ./...`（APP_ENV=test + trademind_test，103 包） | PASS |
| securitytests 全量 -v（permmatrix/idor/shopscope，106 用例含 `TestPermissionMatrix`、`TestViewOnlyPersonaShopWriteSweep` 30 探针） | PASS |
| `pnpm test:backend:integration`（integration + redis db15 隔离） | PASS |
| `pnpm check:dev` / `check:ui-copy --strict` | PASS |
| `pnpm test:frontend`（359）/ `test:contracts`（17）/ `test:collector`（18） | PASS |
| `pnpm build:admin`（prebuild 文档同步产物确认）/ `build:collector` | PASS |
| 全量 Admin E2E（Playwright，chromium） | **359 passed / 3 skipped / 0 failed**（37.3m） |

## 四、Docker 全栈实测（重建 backend+admin 镜像，19/19 PASS）

`docker compose -f docker-compose.full.yml build backend admin` 重建后全栈 up（backend healthy），`seed:demo:full` 灌数据：

- **R57 主链路正向**：demo_admin 标记 DEMO-AT-1004 已付款 → 自动化日志 14 条 → 采购单生成（11 POs），无过度收紧。
- **view-only 修复面 403/40303**：demo_operator 全店 view scope 后，订单同步触发、客服消息同步触发、店铺更新、店铺删除四处写探针均 `403 + code 40303`；权限变更即时吊销旧会话（40101 AUTH_SESSION_REVOKED）亦符合预期。
- **审单整批语义**：view-only 店铺订单批量 approve → 整批 `403/40303` 零生效（#335 定案口径）。
- **40303 文案统一**：上述五处 message 均为「店铺无操作权限」，无旧文案残留。
- **跨租户 404**：demo_tenant2_admin 读租户1订单、删租户1店铺均 `404/40401` 不泄露存在性。
- **MCP/开放 API 抽验**：purpose=both token 创建（明文一次性返回）→ `/api/open/v1/orders` 200/5 行 → MCP initialize + tools/list（4 工具）→ token revoke，全通。
- **R170 新面**：admin nginx `/docs/mcp.md`、`/docs/open-api.md` 均 `200 text/plain; charset=utf-8` 内联可读。
- **双租户 clean/verify**：`seed:demo:full:clean` 后 `verify` 输出 `zero DEMO- residual rows`；测试用 R171-verify token 已吊销、operator scope 已恢复 operate。

## 五、P0/P1/P2

- **P0/P1：无。**
- P2 清单：
  1. `pnpm audit --prod` 仍报 16 项（3 low/8 moderate/5 high，均为 devDependencies 链或已知无升级路径项；axios/immer overrides 已在 main，#248 冗余）——维持既有 baseline 治理，不新增。
  2. E2E 3 skipped 为既有有意 skip（非本轮引入）。
  3. 冗余 OPEN PR #245/#247/#248 建议关闭（内容已在 main，挂着误导积压口径）。

## 六、遗留

- #342 待项目负责人合并（本轮不自行 merge）。
- UX v9 P2-3（finance-report CSV 未折算列口径）仍待产品确认（沿 R170 登记）。
