# R185 线1：R184 新 P2 收口（同机多栈警示）+ 验收包补 R181–R184 增量 + 实跑核对

- 轮次：R185 线1（fullstack-engineer）
- 日期：2026-08-08
- 分支基线：`main`（`135f2c5e`，#368 合并后）叠加 OPEN PR **#369**（R184 P2 收口，含代码）与 **#370**（R184 升级演练报告，纯文档）——两者经权威 PR 状态核实仍 OPEN、mergeable、CI passed，按任务口径叠加后开发（叠加仅 `docs/PROGRESS.md` 文书性冲突，已按双段并存解决）。PR → main，不自行 merge。
- Actions CI 不作依据，结论来自本地门禁与 Docker 实测；证据外置不入库。

## 1. R184 新 P2 收口：同机多栈 `COMPOSE_PROJECT_NAME` 警示

- `docs/production-deployment.md`「二、首次上线」新增**同机多栈警示**：`docker-compose.prod.yml` 顶层硬编码 `name: trademind-prod`，同机第二个 checkout 直接部署会静默共用容器/网络/数据卷（R184 演练实测踩坑）；给出 `COMPOSE_PROJECT_NAME=trademind-drill ./scripts/deploy-prod.sh` 覆盖示例（该覆盖 R184 演练已验证有效）、后续所有 compose 命令需带同一项目名的口径、部署前 `docker volume ls` / `docker ps --filter label=com.docker.compose.project=...` 自查方式。
- `scripts/deploy-prod.sh` 增加**非破坏性冲突检测提示**（评估结论：值得加，且只告警不阻断）：前置检查阶段读取 `com.docker.compose.project=<项目名>` 容器的 `working_dir` 标签，发现同项目名容器来自**其他目录**时打印黄色警示并指向部署文档；不改变任何既有行为（不 exit、不改 compose 参数；正常单栈部署与同目录重跑均不触发）。`bash -n` 语法检查通过。

## 2. 验收包补 R181–R184 增量（`docs/acceptance/ACCEPTANCE_R123.md`）

- §一/21 合入状态收口：#357/#360/#361/#362/#364 已全部合入 main（权威核实），原 ⏳ 条目全部转 ✅。
- 新增 §一/22「R181–R184 增量能力（R185 增补）」8 行：W3 mark-paid 三前提（#363 ✅）、advisory lock 并发限额硬保证（#365 ✅）、大回归 v34（#366 ✅）、R183 安全审计与 #367 审计双写 P1 修复（✅）、竞品 v11 升位超越 5 / 达到 11 / 落后 0（#368 ✅）、R184 审计页权限收紧 + 依赖登记（#369 ⏳ OPEN）、R184 升级演练（#370 ⏳ OPEN）、本轮 R185（⏳）。合并状态均按权威 PR 状态如实标注。
- §三 补 R181–R184 证据索引行；§四 补 R185 增补段（竞品 v11 口径）；§五/14 收口 R181 时点待办并登记 R185 时点待办（#369/#370 待合并）。

## 3. DEMO_SCRIPT 并入 mark-paid 金额上限演示点

- 第 23b 步扩展（约 2 分钟 → 约 2.5 分钟，压缩第 2 步采集列表展示，30 分钟总长不变）：新增第 ⑥ 小步——未配上限 dry_run fail-closed 拒绝 → settings 配置单笔/日累计上限 → dry_run 回显金额/币种/两项上限/当日已用/采购单明细 → execute placed→paid（仅登记线下已付款事实，不调真实支付）；四拒绝路径（超单笔/超日累计/金额差一分/币种不符）口头带过。
- 失实修正：R181 时点「`mark-paid` 刻意留 W3」「需 main 叠加 #361 构建」口径已过时——#360–#368 已全部合入 main，写工具共 6 个（含 W3），基于最新 main 构建 + `MCP_WRITE_ENABLED=true` 即可演示；R185 增补段与 23b 状态列已更正。#369 审计写行 admin-only 为 ⏳ 叠加口径，如实标注。
- 收尾口径补充：演示完除吊销写 token、关租户开关外，还需删除临时 `mcp/mark_paid_*` 上限配置。

## 4. 三角色 + view-only 实跑与 Docker 双租户实测

见 DEMO_SCRIPT「实跑验证记录」2026-08-08（R185 线1）条目（本轮实跑基于 main 叠加 #369/#370 分支构建的全栈镜像；录屏/截图/日志证据外置不入库）。

## 5. 杂项巡检（R184 合入面）

- #369 叠加后代码面核对：`mcpaudit` 写行过滤 SQL 层实现 + `WriteToolNames()` 同源、router 权限注入、UI 列/筛选隐藏与测试齐备；`docs/mcp.md`/`docs/api.md`/`docs/env.md` 口径同步无漂移。
- #370 报告面核对：升级演练证据链完整，新 P2（compose 项目名）即本轮任务 1 已收口。
- `docs/PROGRESS.md` 叠加冲突（R184 线1/线2 两段变更记录并存）已解，追加 R185 线1 变更记录。

## 6. 本地门禁（Actions CI 不作依据）

| 门禁 | 结果 |
| --- | --- |
| backend `go fmt` / `gofmt -l .` / `go vet ./...` | 通过（无差异/无告警） |
| backend `go test ./...` | 通过 |
| `pnpm check:dev` / `pnpm check:ui-copy --strict` | 通过 |
| `pnpm test:frontend` / `pnpm test:contracts` / `pnpm test:collector` | 通过 |
| `pnpm build:admin` / `pnpm build:collector` | 通过 |
| `bash -n scripts/deploy-prod.sh` | 通过 |

## 7. 遗留与建议

- #369/#370 待合并（任意顺序可合，本轮 PR 含其内容叠加，合并后本 PR diff 收敛为纯 R185 增量）。
- 前端工具链跨 major 升级仍待老板决策（`docs/DEPENDENCY_AUDIT_R184.md`）。
