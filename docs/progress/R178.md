# R178 线1：生产升级演练季度复跑（production+Caddy）

日期：2026-08-08。执行人：devops-engineer（Devin）。证据外置于演练机 `~/drill-r178/evidence/`（响应码、容器状态、指纹、备份清单、日志），不入库。

## 演练范围与栈

- 被测栈：`main`（含 R172 后合入面 #346/#347：40303 文案与校验顺序、#352/#356：modal 收口与防重、#353：迁移导入租户闭合 + settings isEncrypted 不降级）+ R177 line2 集成分支 + PR #355（settings 敏感 key 服务端注册表，演练时仍 open，按要求本地叠加）。
- 环境：Docker Compose production（`docker-compose.prod.yml` + Caddy HTTPS，`DOMAIN=localhost`），`STORAGE_PROVIDER=minio`，随机生产密钥，Actions CI 不作依据。

## 1. 从零生产部署计时

- `./scripts/deploy-prod.sh --no-pull` 从零构建+启动+健康检查：**4m45s**（R172：6m39s）；6 服务（postgres/redis/collector/backend/admin/caddy）全部 healthy，Caddy HTTPS `/health-backend` 探活 `appEnv=production` ok，bootstrap 管理员登录成功。
- 部署脚本重跑幂等：fresh 栈 2.8s、drill 栈 3.1s，exit=0，无误重建，服务保持 healthy，override 挂载不丢（R150 口径复验）。

## 2. TRUSTED_PROXIES / OPENAPI_ENABLED 口径复验

- `TRUSTED_PROXIES=172.18.0.0/16`（compose 网段）：外部伪造 `X-Forwarded-For: 7.7.7.7` → `client_ip=172.18.0.1`（不采信）；网内代理链 XFF `8.8.8.8` → 透传。与 docs/env.md 口径一致。
- `OPENAPI_ENABLED`：true 时 openapi token 访问 `/api/open/v1/orders` 200、无 token 401、mcp-purpose token 401；改 false 重启后同 token 404；回 true 恢复 200。

## 3. 存量升级（R172 双租户基线 → R178 叠加栈）

- 基线：旧版本 f6889ebe（R172 时点）从零部署，双租户（tenant 2/3）+3 店铺 + 2 万订单（含跨租户重复单号 DUP-1..5）+4 万订单行 + 6 万库存流水 + 2 万自动化日志（1 万 shop_id NULL）+4000 SKU + 4000 收款 + 5 MCP token + 每租户 1 条加密 settings（isEncrypted:true）与 1 条存量明文敏感项（旧版信任客户端 isEncrypted:false，真实遗留路径）。
- `--pre-upgrade-check`：`/var/backups` 不可写时按文档提示 `BACKUP_DIR` 覆盖后通过（全量备份 7.9MB + 同租户重复订单号预检 0 条）。
- 升级 checkout R178 栈后 `deploy-prod.sh --no-pull`：14.8s（镜像层缓存命中），backend AutoMigrate 正常落地，无迁移错误。
- 数值指纹（11 张表按租户 count/sum/md5）升级前后 **0 差异**，唯一变化为 migrate_round97 预期回填的每租户 3 条 `report_currency` 设置（provider/base_currency/rates），且回填值确定性一致。

## 4. settings 敏感项（#353/#355 重点面）

- 升级后存量加密项仍可解密使用：GET 脱敏回显 `sk-****e222`（脱敏值来自解密后明文，证明解密链路可用）；DB 中密文。
- isEncrypted 不降级：对已加密项用 `isEncrypted:false` 改写 → 仍加密落库、脱敏回显（40303/#353 修复面保持）。
- 注册表对新建项收口（#355）：新建 `platform_shopee/partner_key` 传 `isEncrypted:false` → 服务端强制加密落库、脱敏回显。
- 存量明文敏感项处理口径（实测）：注册表**不回填**存量明文行；GET 仍明文回显；**首次改写即收编加密**（`platform_tiktok/app_secret` 传 isEncrypted:false 改写后密文落库、脱敏回显）。见下方 P2 登记。
- 注册表边界：仅注册表内 key（平台 schema Sensitive 字段 + 集成 schema + email/smtp_password）强制加密；表外 key（如 `platform_shopee/app_secret`，Shopee 敏感字段实为 `partner_key`）沿用客户端声明，兼容口径符合 #355 设计。

## 5. 备份 → 恢复 → 幂等重跑闭环

- S3/MinIO：compose 网内自签 TLS MinIO（`https://r178-minio:9000`，SAN 证书 + `BACKUP_S3_CA_BUNDLE` 经 `docker-compose.prod.override.yml` 挂载，deploy 脚本自动叠加）→ `POST /api/v1/ops/backups` completed、加密（app-master-key）、`uploadStatus=uploaded`，对象 `backups/prod/bk_*.dump.enc`（7.5MB）落桶验证；`POST /ops/backups/:backupId/verify` checksum passed。生产禁 http 端点 fail-fast 复验（`CONFIG_INVALID: BACKUP_S3_ENDPOINT must use https in production`）。
- 本地降级：清空 `BACKUP_S3_*` 后备份 completed、`uploadStatus=skipped`，仅本地产物，不阻塞部署（docs/env.md 口径一致）。
- 恢复：按 upgrade-guide 用 `docker exec -i … pg_restore --clean --if-exists` 恢复升级前 dump，backend 重启后 AutoMigrate 幂等重跑正常，指纹与升级前基线一致（仅确定性 report_currency 回填差异）。

## 6. 修复面抽验（重建镜像后）

- 跨租户 shopId 迁移导入（#353）：租户 B 用租户 A shopId 调 `/imports/validate|commit` → `404/40401 店铺不存在或不可见`；同租户对照进入字段校验（scope 先于 body，#349 口径）。
- 40303：view-only 店铺权限写操作 → `403/40303 店铺无操作权限` 统一 envelope。
- 跨租户资源：GET 他租户 shop/order → `404/40401 not found`，无存在性泄露。

## 结论与登记

- P0/P1：无新发现；文档抽验（upgrade-guide、production-deployment、env.md 备份/override/TRUSTED_PROXIES 口径）与实测一致，无失实需修。
- P2 登记：
  - **P2-R178-1** 存量明文敏感项（注册表内 key，如 `platform_tiktok/app_secret`）升级后 GET 仍明文回显，直到首次改写才收编加密；`sensitive_registry.go` 注释"masked on read, regardless of the client-supplied isEncrypted"对存量明文行不成立。建议：迁移回填加密或读时按注册表脱敏，并修正注释表述。
  - **P2-R178-2**（沿 R176）`pnpm audit --prod` 构建工具链告警继续跟踪，本轮未扩大范围。
- 演练资产：fresh/drill 双栈保留在演练机（`~/drill-r178/`），MinIO 容器 `r178-minio` 为演练专用，可随时销毁。
