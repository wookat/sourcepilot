# R183 线1：安全审计季度复跑（距 R176 六轮）

> 基线：自 `origin/main` 起叠加未合并的 #361 → #363 → #365 → #362 → #366（v34 顺序），目标 main。审计口径见 docs/SECURITY_AUDIT_R183.md 第 0 节。

## 范围

1. MCP 写全链 W1–W3 渗透复验（write:ops scope、三层闸门、dry-run→确认 token→执行、六动作、mark-paid 三前提、advisory lock 限额、治理 UI）。
2. settings 敏感 key 注册表 + 惰性收编回归（明文降级、并发覆盖、Shopee partner_key）。
3. R176 / R181 已修项零回退；`govulncheck`；依赖告警复查。
4. P0/P1 直接修复（先红后绿），P2 列清单。
5. Docker 双租户实测 + 全套本地门禁（Actions CI 不作依据）。

## 结论

- 发现并修复 **1 项 P1**：`procurement_mark_paid` 未登记进 `isWriteTool` 白名单，导致唯一金额型写动作被入口审计中间件**额外**写一行 mode 空的通用审计行（审计轨迹与 mode 筛选失真），且该行写失败会把**已提交**的支付登记回执改写为失败。不影响限额（空 mode 不计入 execute 计数与金额日累计），故非 P0。
- 其余 18 项写面攻击项、settings 注册表 6 项、R176/R181 已修项：未发现 P0/P1，无回退。
- `govulncheck ./...` 0 可达漏洞；`pnpm audit --prod` 16 项（全部 admin 构建链，R176 为 15 项）。
- 4 项 P2/建议列清单（非 PostgreSQL 限额硬保证、读工具审计时序、审计读接口权限位、构建链依赖告警）。

## 改动

- `backend/internal/modules/mcpserver/write_tools.go`：`isWriteTool` 补 `ToolProcurementMarkPaid`（一行修复）。
- `backend/internal/modules/mcpserver/r183_write_audit_test.go`：新增 `TestWriteToolsAuditExactlyOncePerCall`（先红：4 行 ≠ 2 行；后绿通过）。
- `backend/internal/modules/mcpserver/r183_write_registry_test.go`：新增 `TestIsWriteToolCoversWholeWhitelist`，今后新增写动作漏登记即失败。
- `docs/SECURITY_AUDIT_R183.md`：审计报告归档。

## 门禁

`gofmt -l` / `go vet` / `go build` / `go test ./...`（含 APP_ENV=test + Docker PostgreSQL + 测试 Redis DB15）/ `RacePostgres -count=3` / `govulncheck` / `pnpm check:dev` / `check:ui-copy --strict` / `test:frontend` / `test:contracts` / `test:collector` / `build:admin` / `build:collector` 全部通过；`pnpm audit --prod` 16 项告警见报告 P2-4。证据文件作附件不入库。

## 下一步

- 报告 P2 清单进入后续轮次排期；本轮 PR 不自行 merge。
