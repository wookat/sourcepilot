# 第 173 轮线1：40303 文案全站回归 + R172 P2 收口（fullstack-engineer）

- 基线：main + #346（`fix/round172-40303-copy-unify`，OPEN，已叠加，PROGRESS.md keep-both 冲突已解）；#342/#343/#344/#345 均已合入 main。
- 口径：本地全套门禁 + Docker 双租户实测；Actions CI 不作依据；证据外置作附件不入库。

## 1. 任务1：40303 文案全站回归（grep + 权限探针双口径）

- grep 口径：全站扫描 `无操作权限|无该店铺的操作权限|无权操作` 等旧/新措辞变体（backend/admin/collector 生产代码，排除测试）。结果：**统一文案「店铺无操作权限」全覆盖**，含后端全部 40303 envelope（adminperm/deny、security/authorize、order、procurement、finance、customerchat、productpublish、orderexception、operationtask、inventorysyncp9、migrationimport 等）与前端审单 Tooltip（`admin/src/pages/Orders/Review/index.tsx`）。
- 唯一旧措辞残留：`backend/internal/modules/migrationimport/commit_payments.go:136`「当前账号无该店铺的操作权限」——为迁移导入**行级失败展示文本**，非 40303 envelope，按任务书仅登记不改（见 §3）。
- 探针口径：permmatrix `requireCode40303` 已断言 message 必须为「店铺无操作权限」（#346 增强），本轮 permmatrix 全量探针（含 view-only 写探针 sweep）全绿；未发现新漏网，无需新修复。

## 2. 任务2③：异常 handle/ignore 校验顺序收口（先 scope 后 body）

- 现状（收口前）：`orderexception/handler.go` Handle/Ignore 均为先校验 body（`exceptionType` 缺失 → 400）后校验 scope，与全站「先 scope 后 body」口径不一致，空 body 会向 view-only/不可见主体泄露 400 而非 403/404。
- 收口：Handle/Ignore 统一调整为 `denyScope` → `ensureSourceOperable` → body 校验。
- 回归测试（先红后绿）：新增 `backend/internal/securitytests/permmatrix/r173_exception_scope_order_test.go`（`TestExceptionMarkScopeBeforeBody`），覆盖 handle/ignore × 空 body 三档：view-only 店铺 → 403+40303+统一文案；无授权（不可见）店铺 → 404 不泄露存在性；可操作店铺 → 400 `exceptionType required`。修复前实测红（view-only 空 body 返回 400），修复后全绿。

## 3. 任务2①：迁移导入行级失败文本（登记，不改行为）

- `commit_payments.go:136` 行级失败文本「当前账号无该店铺的操作权限」保持原样。**待产品确认**是否与 40303 envelope 统一为「店铺无操作权限」；确认前不改行为（行级展示语义与 envelope 文案可能有意区分）。

## 4. 任务2②：docs/progress/R171.md 缺档核实

- R172 P2② 登记时 R171.md 尚未入库；经权威 PR 状态核实，**#344（R171 线1全站大回归 v30）已 MERGED**，`docs/progress/R171.md` 已随之在 main（commit `50909ac7`），另有 `R171-line2.md`。**缺档已闭环，无需补档**，本轮仅归档核实结论。

## 5. 门禁与实测

- backend：`gofmt -l` 空、`go vet ./...` 过、`go test ./...` 全绿（含 securitytests/permmatrix 全量，测试库 `trademind_test`）。
- 前端/工程：`pnpm check:dev`、`pnpm check:ui-copy --strict`、`test:frontend` 359、`test:contracts` 17、`build:admin` 均过。
- Docker 双租户实测：compose 栈（postgres/redis）+ 后端实测双租户边界——view-only 空 body handle/ignore → 403+40303 统一文案；跨租户/不可见 → 404；可操作 → 400。证据外置作附件。

## 6. 结论与遗留

- P0/P1：无。任务1回归未发现新漏网；任务2③已收口并有先红后绿回归测试锚定。
- P2：迁移导入行级失败文本待产品确认（§3）；#346 仍 OPEN，本轮 PR 以其分支叠加为基线，建议先合 #346 再合本轮 PR（或直接合本轮 PR 即含 #346 内容）。
