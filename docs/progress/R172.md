# 第 172 轮线1：生产部署演练季度复检（devops-engineer）

- 基线：R168 线2 演练（`docs/progress/R168-line2.md`）；本轮 main 顶点 `f6889ebe`（R168 后 #324–#341 已全部合入）。
- 演练环境：本机 Docker（production compose + Caddy，`DOMAIN=localhost` 自签），全新隔离目录，两套栈：fresh（最新 main 从零部署）与 drill（R168 时代 `d645ec96` 起步升级）。证据以附件归档，不入库。

## 1. 从零部署最新 main（production+Caddy）

- `./scripts/deploy-prod.sh --no-pull` 从零部署 **6m39s（<15min 达标）**，六服务（postgres/redis/collector/backend/admin/caddy）全部 healthy，`/health-backend` database/redis ok，`appEnv=production`。
- `TRUSTED_PROXIES=172.18.0.0/16` 口径复验：外部伪造 `X-Forwarded-For: 7.7.7.7` → 日志 `client_ip=172.18.0.1`（不透传）；compose 网内（admin 容器）带 `X-Forwarded-For: 8.8.8.8` → `client_ip=8.8.8.8`（受信透传）。与 R168 记录一致。
- `OPENAPI_ENABLED` 口径复验：无 token 401；openapi-purpose token 200；mcp-purpose token 打开放 API 401；`OPENAPI_ENABLED=false` 重启后 404，恢复 true 后 200。与文档一致。
- `deploy-prod.sh` 原地重跑幂等：第二次运行 3.6s 完成、exit=0、服务不重建、HTTPS 探活通过。

## 2. R168 时代基线（双租户存量）升级到最新 main

- drill 栈按 `d645ec96`（R168 演练顶点系）从零部署（5m44s），构造双租户基线：租户 2/3、店铺 A1/A2（租户2）+ B1（租户3）、订单 20000（含跨租户重复单号 DUP-1..5）、订单项 40000、库存流水 60000、自动化日志 20000（其中 10000 条 `shop_id` 为 NULL 模拟存量）、SKU 4000、回款 4000、MCP token 5、话术模板 20 + 多语言变体 20、view-only 运营（店 A1 view 授权）。
- 升级：checkout `f6889ebe` 后 `deploy-prod.sh --no-pull` 重建镜像并部署（24.6s，Docker build cache 命中），backend AutoMigrate 落地无错误；`order_automation_logs.shop_id` 回填 UPDATE 命中 10000 行（NULL → 0）。
- 数值指纹（9 张业务表 count/sum/md5 全行摘要）升级前后 **0 差异**（除预期 shop_id 回填外无任何数据漂移）。
- view-only 修复面六处实测（升级后新镜像）：审单 approve/reject、异常 handle/ignore、店铺删除、店铺授权写、手动 sync-orders、刊登目标店草稿 —— 全部 **403 + 40303**，零落库。
- 跨租户实测：读/删他租户店铺、他租户 sync-orders、他租户异常标记 → **404 + 40401**；跨租户批量审单 200 + 行级「订单不存在」（不泄露存在性），与 R166/R167 定案一致。
- **P1（本轮唯一 P0/P1）**：round168「40303 文案统一（全站一致）」记载失实——实测异常 handle/ignore 与刊登目标店两面返回旧文案「当前账号无该店铺的操作权限」。溯源为 `productpublish`/`finance`/`orderexception`/`migrationimport` 四处漏网。本轮 PR 收口为统一文案，并在 `permmatrix.requireCode40303` 增加 message 回归断言；`docs/permission-matrix.md` 增补 round172 节。

## 3. --pre-upgrade-check 与备份→恢复→幂等重跑

- 非 root 默认 `/var/backups`：明确失败并给出 `BACKUP_DIR` 覆盖指引（与 upgrade-guide 一致）；`BACKUP_DIR` 覆盖后：全量 pg_dump 备份（8.7MB）+ 同租户重复订单号预检通过（跨租户 DUP-1..5 正确不告警）。
- 恢复：`pg_restore --clean --if-exists` 导入升级前 dump（0 错误）→ backend 重启 AutoMigrate 重新落地（shop_id 回填重现）→ 指纹与升级后完全一致；再次重启幂等（0 error/panic，指纹 0 差异）。
- S3/MinIO：compose 网内自签 TLS MinIO（`https://minio:9000` + `BACKUP_S3_CA_BUNDLE` 挂载，走 `docker-compose.prod.override.yml` 自动叠加）→ `POST /api/v1/ops/backups` 备份 completed、加密（app-master-key）、`uploadStatus=uploaded`，对象 `backups/prod/bk_*.dump.enc` 落桶验证。
- 本地降级：清空 `BACKUP_S3_*` 与 bucket 后重启，备份 completed、`uploadStatus=skipped`（仅本地路径），不阻塞启动，与 `docs/env.md` 降级口径一致。

## 4. P2 清单（不阻塞）

1. R168 P2① 文案统一在 round168 收口后仍有四处漏网（本轮已修，升级为 P1 处置）；迁移导入行级失败文本（`commit_payments`）仍为旧措辞，属行级展示非 40303 envelope，待产品确认是否统一。
2. 任务书要求阅读 `docs/progress/R171*`，仓库中不存在该文件（仅 git 历史 merge 信息提及 R171 线2），建议补档或修正引用。
3. 异常 handle/ignore 仍为「先校验 body（exceptionType）后校验 scope」，与 R168 P2② 记录一致，未回退也未收口。

## 5. 结论

- 从零部署 <15min、TRUSTED_PROXIES/OPENAPI_ENABLED、升级迁移（AutoMigrate + shop_id 回填 + 指纹 0 差异）、view-only/跨租户边界、pre-upgrade-check、备份恢复（S3/MinIO + 本地降级）、部署幂等全部通过。
- 唯一 P0/P1：40303 文案统一记载失实（四处漏网），已随本轮 PR 收口（代码 + 回归断言 + 文档）。
