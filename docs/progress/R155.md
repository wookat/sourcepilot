# Round 155 线1：v25 P2×4 收口 + 合并期杂项

日期：2026-08-07。承接 R154 全站大回归 v25 集成报告 P2 清单（§四，4 项；报告登记于会话附件，交叉来源：PR #313/#312 与 `docs/progress/R154.md`）。基线：最新 main（合至 #302）；#303–#313 尚未合并，本轮涉及 #307 验证面时按需本地叠加（见 §2）。

## 1. v25 P2 逐项处置（P2×4）

### P2-1 `GET /api/health` 404

- 核对结论：**登记不改（非缺陷）**。全仓（docs/README/compose/Caddy/前端）无任何文档或配置声明 `/api/health` 路径；健康检查规范路径为 `GET /health`、`GET /healthz`、`GET /api/v1/health`（综合）与 `/health/live`、`/health/ready`，`docs/api.md` §公共约定与 §运维/健康检查表均已正确列出。容器 healthcheck 用 `/health`，正常。`/api/health` 404 属未定义路径的预期行为（统一 NoRoute envelope）。

### P2-2 #307「审计卡创建后自动轻刷新」时序（v25 仅部分验证）

- 本轮补完整验证，见 §2。结论：**验证通过，无需修复**；补充 2 个确定性回归测试（PR #314，base=#307 分支，随其合入）。

### P2-3 登录 API body 字段为 `account` 非 `email`

- 处置：**文档收口**。`docs/api.md` 认证表 `POST /api/v1/auth/login` 行补 body 形状 `{ "account": "邮箱或手机号", "password": "..." }` 并显式注明字段名为 `account`。后端 `binding` 与前端 `services/auth.ts` 本就一致，无代码变更。

### P2-4 承接 #311 已登记未修 P2（both token 双入口双份额度、401/429 不写审计、分页非法参数归一化、operator regenerate 404 vs 403）

- 核对结论：**随 #312/#303 闭合，本轮登记不重复实现**。#312（open，→ main）已修 401/429 审计行（`auth_failed`/`rate_limited` + 节流）与分页非法参数 400，并登记 both 额度（产品决策）与 404 口径（防资源存在性泄露，维持）；token 上限竞态与 MCP 审计 fail-closed 随 #303 闭合。若 #312/#303 回退需重新排期。

## 2. #307 审计卡轻刷新时序完整验证（v25 遗留）

v25 Docker 实测因「创建时该租户尚无对比数据」仅部分验证。本轮验证：

- **单元级（确定性时序）**：本地叠加 #307（main + `fix/round151-misc`）后，`McpTokens.test.tsx` 新增 2 用例——①已有对比数据时新审计行入库后点「刷新」：响应在途期间旧行保持可见、不闪回「暂无数据」，返回后新旧行同现；②迟到的旧请求**报错**不得覆盖后发请求的成功结果（不显示错误 Alert）。两用例在 main（无 #307 护栏）下按预期失败（含原有用例共 2 failed），叠加 #307 后 3/3 通过——客观证实 `auditSeqRef` 护栏覆盖成功/失败两条迟到路径。测试以 PR **#314**（base=`fix/round151-misc`，随 #307 合入）提交。
- **Docker 全栈实测**：见 PR 测试方式与外置证据（不入库）。先以工具调用制造对比审计行，再新增审计行后点「刷新」，确认新行出现、无「暂无数据」闪回、无整页 reload；连点「刷新」不闪烁。

## 3. 合并期语义冲突预案（#308 `mcptoken.Create` purpose 参数）

- 现状核对：#303/#305/#307/#308/#309/#310/#311/#312/#313 均未合并。#308 给 `mcptoken.Create` 增加 `purpose` 参数后，#303 `concurrency_test.go` 与 #311 `audit_test.go` 的旧签名调用会 `go vet` 编译红；修复为调用处补 `""`（默认 mcp 用途）。
- 处置：**未合并，仅登记提醒**（本 PR 描述 + 本文件）。届时按 v25 建议顺序（#303→#305→#307→#308→#309→#311→#310→#306→#304）合入时，#308 合入后修 #303 遗留调用、#311 合入自带修复；若先行合并出现红灯，按上述口径直接修复。

## 4. 验证

- 本分支（fix/round155-misc）为纯文档变更：链接/路径/命令核对通过；`pnpm test:contracts`、`pnpm check:ui-copy --strict` 回归确认。
- #314 分支：`pnpm test:frontend`（含新增 2 用例）、`pnpm check:dev`、`pnpm build:admin`。
- Docker 全栈实测证据外置不入库；Actions CI 不作为验收依据。

## 5. 边界与遗留

- P2-1/P2-4 为登记项，无代码变更；P2-4 实际闭合依赖 #312/#303 合入。
- purpose 语义冲突提醒在 #303–#313 批量合入完成后自然失效。
