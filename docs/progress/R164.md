# Round 164 线1：生产部署演练季度复检（R159 后合入面）

日期：2026-08-07。演练环境：独立副本（production compose + Caddy，`DOMAIN=localhost` 自签路径），演练 main 为 `a78e2fb0`（#324 批次；演练后 main 仅新增 #325，为 E2E spec + 文档变更，不影响本轮结论）。证据文件（部署日志、指纹、响应）留存演练机 `/tmp/r164/`，按惯例不入库。

## 1. 从零部署（<15 分钟）

- `./scripts/deploy-prod.sh --no-pull`：从零（无镜像缓存）构建 + 启动 + 健康检查 **236 秒**；R159 时代版本（`32a9aaea`）从零 194 秒；升级部署（镜像缓存命中）17 秒。均远小于 15 分钟。
- postgres / redis / collector / backend / admin / caddy 六服务全部 healthy；`curl -fsSk --resolve localhost:443:127.0.0.1 https://localhost/health-backend` 返回 `code=0, status=up`（database/redis/6 队列/13 worker 全 ok），admin 首页 HTTPS 200。

## 2. TRUSTED_PROXIES / OPENAPI_ENABLED / XFF（#316 口径）

- `.env`：`TRUSTED_PROXIES=172.18.0.0/16`（实测 compose 网络即 172.18.0.0/16）、`OPENAPI_ENABLED=true`。
- XFF：外部请求伪造 `X-Forwarded-For: 6.6.6.6` 经 Caddy→admin 链，后端 `client_ip=172.18.0.1`（Caddy 丢弃不可信 XFF，伪造 IP 不落地）；可信网段内（admin 容器）带 `X-Forwarded-For: 9.9.9.9` 直连 backend，`client_ip=9.9.9.9` 正确落地。与 `.env.prod.example` / docs 口径一致。
- OPENAPI 开关：`true` 时 `/api/open/v1/*` 无 token 401；改 `false` 重启后同路径 404；改回 `true` 恢复 401/200。

## 3. R159 双租户存量基线升级

基线（`32a9aaea` 上构造，口径对齐 R159 线2）：双业务租户，20000 订单（含跨租户重复订单号 DUP-1..5）、40000 订单行、60000 库存流水、20000 自动化日志（半数 shop_id 为 NULL 模拟存量）、4000 SKU、4000 回款、5 枚存量 MCP token（purpose mcp/openapi/both 混布）、20 模板 + 20 多语言变体、view-only 店铺授权样本。

- **AutoMigrate**：升级到 `a78e2fb0` 后启动迁移成功，无 ERROR / migrate_failed；`report_currency`（provider/base_currency/rates）settings 按租户 seed 落地；大屏卡片配置按 #318 口径存于 settings `dashboard_screen` 组（无独立新表，符合实现）。
- **数值指纹**：升级前后订单/订单行/库存流水/SKU/回款/token hash/模板 8 类指纹 **0 差异**，唯一变化为预期迁移：`order_automation_logs.shop_id` 回填（NULL 10000→0）。跨租户重复订单号未误报，同租户 0 重复。
- **迁移幂等**：重启 backend 后指纹不变、无错误日志。

## 4. 升级后功能实测

- **view-only 403+40303**：view 店铺授权账号写订单 → HTTP 403 + `code=40303`（店铺无操作权限）；readonly 角色写 → 403 + `40301`；大屏卡片配置写 → 403 + `40301`。
- **40303/400（#323）**：OpenAPI `page=0` / `pageSize=0` → HTTP 400 + `40001`（不再静默归一化）；`pageSize=1000` 钳制 100。
- **大屏折算/自定义指标（#318）**：未配汇率时 USD/EUR/THB 全部列 `unconvertedCurrencies`、不计入 `salesBase`；配置 `{"USD":"7.2","EUR":"7.8"}` 后 USD/EUR 折算进合计、THB 保持未折算口径。卡片配置读/写/禁用/排序生效（kpi_profit、todos 关闭后 GET 回读一致）。
- **OpenAPI/MCP**：租户 admin 创建 purpose=openapi token 成功，`/api/open/v1/orders` 200 且仅返回本租户数据（total=10000）；审计 `mcp_tool_call_logs` 有记录；无 token 401。
- **币种设置未保存/离开确认**：当前 main 仅有 R158 的未保存 Tag（`report-currency-unsaved`），**无路由离开确认**——该能力在 PR #327（R162 线2 UX v10）中，截至本轮 **#327 尚未合入 main**（任务背景中「#327 已合入」与仓库事实不符，见 §7）。

## 5. --pre-upgrade-check 与备份→恢复→幂等重跑

- 成功路径：pg_dump 备份生成（8.9MB 非空）、同租户重复订单号预检 0 行通过。
- 可写校验（#317，最新 main 实测）：目录存在但不可写 → 「备份目录 … 不可写（当前用户 ubuntu）：请以 root 运行，或用 BACKUP_DIR=<可写目录> 覆盖」；目录无法创建 → 同口径清晰报错。R159 P2①（裸报 pg_dump 失败）已闭环；旧版 `32a9aaea` 复测仍为裸报，证实为 #317 修复。
- 备份→恢复→幂等重跑：停 backend → `pg_restore --clean --if-exists` 恢复升级前 dump → 指纹与升级前完全一致 → 重启 backend 触发 AutoMigrate → shop_id 再次回填为 0 NULL、指纹与首次升级后一致、健康检查通过、无错误日志。恢复会回退备份点之后新建的 token（本轮演练新建的 2 枚 openapi token 被回退），与 R159 P2③ 口径一致，升级前备份点选择需注意。

## 6. 文档/env 示例核对

`.env.prod.example`（TRUSTED_PROXIES 注释、OPENAPI_ENABLED、备份段）、`docs/production-deployment.md`、`docs/production-launch-checklist.md`（BACKUP_DIR 口径）、`docs/upgrade-guide.md`（备份→预检→换镜像→验证顺序、迁移清单）与实测行为一致，**未发现 P0/P1 失实**，本轮无需代码修复 PR。

## 7. P2 清单（不改代码，仅登记）

1. `PUT /api/v1/settings` 写 `report_currency.rates` 时，JSON 数值型汇率（如 `{"USD":7.2}`）被静默忽略（`fxrate.ParseRate` 仅接受字符串精确小数），接口 200 但折算不生效、无校验报错；前端页面走字符串路径不受影响，直连 API 易踩坑。建议补 400 校验或文档注明。
2. 任务背景与仓库事实不符：#325 已于演练后合入（无运行时影响）；**#327（币种设置未保存离开确认 UX v10）仍未合入 main**，「离开确认实测可用」无法在 main 上成立，建议合入后下轮补验。
3. 恢复升级前备份会回退备份点后新增数据（token 等），沿 R159 P2③ 口径，操作手册已有提示，继续保留登记。

## 8. 结论

从零部署、双租户存量升级、迁移幂等、指纹 0 差异（除预期回填）、view-only/readonly 权限、40303/400、大屏折算与自定义指标、OpenAPI 租户隔离与审计、XFF/开关口径、--pre-upgrade-check 可写校验、备份恢复闭环全部通过；无 P0/P1 文档失实；P2 3 项如上。
