# R170 线1：P2 批次收口 + 验收包增量（fullstack-engineer）

- 日期：2026-08-08
- 基线：main `9a2c566d`，按序叠加未合并 #340（`review/r169-ux-v11`）与 #339（`devin/1786143308-r169-line2`）后开工（两者均为纯文档报告）。
- 范围：R169 线2 P2×4 逐项收口、UX v10 P2-3 收口、验收包补 R167–R169 增量、DEMO_SCRIPT 口径更新、ui-copy strict + Docker 实跑 + clean/verify。

## 一、R169 线2 P2×4 收口

| # | 事项 | 处理 |
| --- | --- | --- |
| P2-1 | MCP 分页钳制语义与开放 API 400/40001 口径不一致 | **已文档化**：`docs/mcp.md` 新增「分页参数口径（与开放 API 的差异）」小节，明示钳制规则（page<1→1、pageSize<1→20、>100 截断）、差异原因（LLM 客户端减少报错重试 vs 程序化调用尽早暴露错误）与客户端注意事项。不统一行为：两入口面向的调用方不同，属有意差异。 |
| P2-2 | Redis 停机期每请求先等 Redis 超时再降级，延迟放大 | **已修复**：`ratelimit.RedisLimiter.run` 增加 200ms 每次调用超时上界（select + 后台丢弃口径；共享 go-redis 客户端默认 `ContextTimeoutEnabled=false` 且保留多秒级超时给队列用途，故在限流层自行兜底）。新增回归测试 `TestRedisLimiterStalledRedisFallsBackQuickly`（accept 后不响应的「卡死 Redis」，3 次调用需在 2s 内完成且 fallback 限流语义不变）。不 fail-open 语义不变。 |
| P2-3 | `mcp_api_tokens` 上限计数含租户 0 路径无单测 | **已补测**：`mcptoken/service_test.go` 新增 `TestCreateCapTenantZero`——租户 0 建满 20 个后第 21 个返回 `ErrTooManyTokens`、且不占用其他租户额度、List 按 `tenant_id=0` 精确计数。 |
| P2-4 | permmatrix 套件依赖 `TEST_DATABASE_URL` + `APP_ENV=test` 手工配置 | **登记保留（有意设计）**：`APP_ENV=test` 已由 harness `t.Setenv` 自动设置，剩余 `TEST_DATABASE_URL` 不做默认值——安全测试库绝不 fallback 到开发库（project-testing SKILL §10 口径），未配置时显式 skip 并指向 `docs/permission-matrix.md`；CI 由 service 容器提供。不改代码。 |

## 二、UX v10 P2-3 收口（mcp-tokens 页文档链接）

- 方案（避免硬编码仓库地址）：**站内自托管**——新增 `scripts/sync-admin-docs.mjs`，admin `predev`/`prebuild` 时把 `docs/mcp.md`、`docs/open-api.md` 复制到 `admin/public/docs/`（gitignore，单一来源仍在 `docs/`），构建产物在 `/docs/mcp.md`、`/docs/open-api.md` 可访问；nginx 增加 `/docs/` location 以 `text/plain; charset=utf-8` 内联展示（否则 .md 会被当 octet-stream 下载）；`admin/Dockerfile` + `.dockerignore` 补 docs 两文件进构建上下文。
- 页面：`McpTokens.tsx` 说明文字中 `docs/mcp.md` / `docs/open-api.md` 改为 `Typography.Link`（`target="_blank"`），新增单测断言链接与 href。

## 三、验收包增量（R167–R169）

- `ACCEPTANCE_R123.md`：§一/18 合入状态收口（#332/#333/#334 已合入）；新增 §一/19「R167–R169 增量能力」7 行（审单整批 403 定案 #335、验收包增量 #336、40303 文案统一 #337、升级演练复跑 #338、UX v11 #340 OPEN、token 治理复查 #339 OPEN、R170 本轮收口行——合并状态如实标注）；§三补 R167–R169 证据索引；§五补 R170 时点待办（#339/#340 待合并、permmatrix 手工配置保留理由、v9 P2-3 待产品确认）。
- `DEMO_SCRIPT.md`：标题与增补段补 R170 口径（第 21b 步 view-only 预禁用口径、第 23 步文档链接可点击、MCP/开放 API 分页口径差异口头带过），不新增步骤，30 分钟总长不变；实跑记录见下。

## 四、验证

- backend：`gofmt -l` 干净、`go vet`、`go test ./internal/pkg/ratelimit/... ./internal/modules/mcptoken/...` 通过（全量 go test 见 PR 门禁外本地跑批）。
- 前端/文案：`pnpm check:ui-copy --strict`、`pnpm test:frontend`、`pnpm build:admin` 通过（见 PR 描述）。
- Docker 全栈实跑 + 三角色核对 + clean/verify 零残留：见 `DEMO_SCRIPT.md`「实跑验证记录」2026-08-08（R170 线1）条目；证据（录屏/截图）外置不入库。

## 五、遗留

- #339/#340 纯文档 PR 待合并（内容已叠加进本轮分支）。
- UX v9 P2-3（finance-report CSV 未折算列口径）仍待产品确认。
- MCP 写白名单（#326 方案）、operator 管 MCP token 收紧、前端工具链跨 major 升级仍待老板决策。
