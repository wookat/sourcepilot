# R166 线1：全站大回归 v29 — view-only 安全大批修复（#330+#331+#332）合入前最终集成验证

- 日期：2026-08-07
- 角色：qa-engineer（company-os）
- 分支：`integration/r166-regression-v29`（基于最新 main `509637b9`）

## 结论

**通过。** #330（含 #331 六处 P1 修复）+ #332 叠加 main 后全部门禁绿（permmatrix 30 探针 sweep、viewOnlyOperator 113 契约行、r165 securitytests、backend 全量、前端/契约/构建、全量 E2E 358 passed），Docker 全栈重建镜像实测 R57 主链路无过度收紧、view-only 六处修复面 403/40303 抽验通过、跨租户一律 404、双租户清理零残留、三角色三视口 UI 正常。无 P0/P1；P2 共 2 项（见下）。

## 集成顺序与冲突结论

| PR | 状态 | 处理 |
| --- | --- | --- |
| #324 / #325 / #327 / #328 / #331 | 已合并 main | 跳过（#331 已并入 #330 分支历史） |
| #330（round164-line2 会话写面收口） | 未合并 | 第 1 个叠加，**无冲突** |
| #332（round165-line1 view-only 全站扫尾，base=#330 分支） | 未合并 | 第 2 个叠加，**13 文件冲突**（见下） |
| #329（round164 生产部署演练文档） | 未合并 | 最后叠加，仅 `docs/PROGRESS.md` 变更记录区冲突，按轮次序并集解决 |

最终建议合并顺序：**#330 → #332 → #329**（#332 合并时 GitHub 会自动 retarget 到 main）。

#332 冲突根因：main 已合入 #331（线2 安全审计的同面修复），与 #332（线1 全站扫尾）对同一批写路径做了双工修复，实现命名/口径略有差异：

- `ordersync`/`customersync`/`productpublish`/`shop`：双方语义等价，保留 main（#331）侧 helper（`adminperm.FailStoreWriteScope`、`findScopedShopForWrite` 等），并修正 `douyin_oauth.go` 对旧 helper 名的引用。
- `order/review_workbench.go`：**语义冲突**——#331 为批量前置 `ensureReviewBatchOperable` 整批 403/40303，#332 为 200 envelope + 行级失败。**R166 定案取 #331 口径（整批 403，避免部分生效）**，行级 `EnsureStoreOperable` 保留为纵深防御；同步修正 #332 的 `view_only_sweep_test.go` 审单断言与 `docs/permission-matrix.md` 口径说明。
- `docs/PROGRESS.md`、`docs/permission-matrix.md`：变更记录/章节并集。

## 门禁结果矩阵

| 门禁 | 结果 |
| --- | --- |
| permmatrix `TestPermissionMatrix`（含 viewOnlyOperator 113 契约行） | PASS |
| permmatrix `TestViewOnlyPersonaShopWriteSweep`（30 写探针 + 零落库 + 404） | PASS |
| permmatrix r165 六用例（审单/异常/店铺删除/授权+OAuth/同步/刊登目标） | PASS |
| securitytests（idor / permmatrix / shopscope） | PASS |
| backend `go build` / `go vet` / `gofmt` / `go test ./...` | PASS |
| backend integration + redis（`pnpm test:backend:integration`） | PASS |
| `pnpm check:dev` / `check:ui-copy --strict` | PASS |
| `pnpm test:frontend`（358）/ `test:contracts` / `test:collector` | PASS |
| `pnpm build:admin` / `build:collector` | PASS |
| 全量 E2E（Playwright，361 用例：358 passed / 3 skipped） | PASS |

## Docker 全栈实测（重建镜像）

- R57 主链路正向：demo_admin 订单/自动化规则新建编辑/自动化日志/买家消息/选品/运营任务审批/采购单全部可用；demo_operator 授权店写操作（订单操作、店铺更新、审单）可用——**无过度收紧**。
- view-only 六处修复面（demo_viewonly，仅 view 授权）：审单 approve/reject、异常 handle/ignore、店铺删除、店铺授权 PUT + 抖店 OAuth refresh/revoke、sync-orders / sync-customer-messages、刊登 publish / create-drafts 全部 **403 + 40303**。
- 跨租户（新注册租户管理员访问租户 A 资源）：shop/order 读写、同步、OAuth 一律 **404**；审单批量对不可见订单行级「订单不存在」；对方租户列表零可见。
- 双租户零残留：租户 1 + 4 各有 DEMO- 数据，`seed:demo:full:clean` 后 `verify` 输出 zero DEMO- residual rows，DB 复核 shops/orders DEMO- 均为 0。
- 三角色三视口（1440×900 / 1024×768 / 375×812）：无横向溢出、readonly 写按钮隐藏、375 底部导航正常；view-only 数据可见、写入口隐藏/禁用、不白屏不崩溃。

## P0/P1

无。

## P2 清单

1. **view-only 点订单「同步」重试无前端提示**：后端正确 403/40303 拒绝且零落库，但 UI 无可见「店铺无操作权限」toast，用户无反馈（UI 走查发现）。
2. **审单批量语义双口径文档漂移**：#332 原文档描述「200 envelope + 行级失败」，R166 集成后定案为整批 403/40303；`docs/permission-matrix.md` 已在本分支修正，#332 合并前建议同步其 PR 描述避免误导。

## 证据

- 门禁/E2E/Docker 日志、API 抽验输出、UI 录屏与截图作为会话附件提供，不入库。
