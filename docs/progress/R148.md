# Round 148 线1：R144–R147 验收收口——MCP 页时间列 formatDateTime + 验收包增量 + Docker 实跑

日期：2026-08-06。承接大回归 v23 报告 P2（MCP 管理页时间列 ISO 直出）与 R148 线1 任务（验收包增量并入 R144–R147 交付）。基线：最新 main（#294–#300 均已合入，无需本地叠加、无「待合并」条目）。

## 1. MCP 管理页时间列 ISO 直出收口（v23 P2）

- `admin/src/pages/Settings/McpTokens.tsx`：
  - token 列表「过期时间」列与创建结果弹窗的 `expiresAt` 由 ISO 直出改为 `formatDateTime`（`YYYY-MM-DD HH:mm:ss`，空值「—」）。
  - 审计日志卡片「时间」列 `createdAt` 补 `render: formatDateTime`。
- 同类残留扫描：大屏页 `Dashboard/Screen/index.tsx`（`generatedAt`/`occurredAt`）已用 `formatDateTime`，MCP/大屏面无其他 ISO 直出残留。
- 回归测试：`round144-mcp-tokens.spec.ts` 新增用例——mock 返回 ISO `expiresAt`/`createdAt`，断言页面显示 `YYYY-MM-DD HH:mm:ss` 格式且原始 ISO 串不出现，写请求 0。

## 2. 验收包增量（R144–R147 并入）

- `docs/acceptance/ACCEPTANCE_R123.md`：新增 §一/14「R144–R147 增量能力」5 行（MCP 只读入口+token 治理 #294/#295、实时经营大屏 #296、MCP 安全加固 #298、R146 QA/R147 杂项收口 #297/#299/#300、大回归 v22/v23 证据与 v23 P2 收口），全部 ✅（无 ⏳ 待合并）；§三证据索引与 §五遗留补 R148 时点结论。
- `docs/acceptance/DEMO_SCRIPT.md`：新增第 1b 步实时经营大屏（限时约 1 分钟）；第 23 步治理面收尾并入 MCP 只读接入演示点（seed 的 DEMO-MCP 脱敏样本 + 现场创建 token 用 curl/MCP Inspector 调 `tools/list`+`orders_query` + 审计日志卡片 + 吊销即 401）；采购支付状态/渠道中文化在第 14 步口头带过。总时长保持约 30 分钟（大屏并入第 1 段 6 分钟、MCP 不新增独立段）。

## 3. 验证

- 门禁：`pnpm check:dev`、`pnpm check:ui-copy --strict`、`pnpm test:frontend`、`pnpm test:contracts`、`pnpm build:admin`、`pnpm build:collector`、backend `go fmt`/`go vet`/`go test ./...`（本轮未改 Go 代码，作为回归确认）；受影响 E2E：`round144-mcp-tokens.spec.ts`（含新增用例）、`round145-dashboard-screen.spec.ts`。结果见 PR。
- Docker 全栈实跑（按更新后 DEMO_SCRIPT，证据外置不入库）：结果见 `docs/acceptance/DEMO_SCRIPT.md`「实跑验证记录」2026-08-06（R148 线1）条目。

## 4. 边界与遗留

- 大回归 v22/v23 报告为证据类交付不入库（对应会话/PR 评论）；本轮收掉 v23 唯一 P2。
- Actions CI 不作为本轮验收依据，以本地门禁 + Docker 实测为准。
- 凭证 ①（抖店）注入后插队真实 E2E 仍为正式验收前唯一外部前置项（口径同验收包 §五）。
