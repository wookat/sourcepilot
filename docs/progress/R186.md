# R186 线1：全站大回归 v35

- 轮次：R186 线1（qa-engineer）
- 日期：2026-08-08
- 分支基线：`main`（`f9695c86`，#369/#370 合并后）叠加 OPEN PR **#371**（R185 线1：部署警示 + 验收包，`devin/1786200557-r185-line1`）与 **#372**（R185 线2 UX v13：mark-paid 限额 UI 表单 + 裸枚举中文化，`devin/1786201500-r185-line2-ux-v13-pr`）。两者经权威 PR 状态核实为 OPEN、mergeable，按任务口径以 #371→#372 顺序本地叠加（仅 `docs/PROGRESS.md` 文书性冲突，双段并存解决）。PR → main，不自行 merge。
- Actions CI 不作依据，结论来自本地门禁与 Docker 全栈实测；证据（矩阵 JSON、脚本、截图、门禁日志）外置不入库。

## 1. 结论

**全部门禁通过、Docker 全栈场景矩阵 60/60 项断言 PASS，P0/P1 = 0，无需修复；P2 无新增。** 合并顺序结论：**#371 → #372 → 本 R186 集成 PR**（本分支即三者叠加终态，前两者合入后本 PR diff 收敛为纯 R186 报告增量）。

## 2. 本地门禁（Actions CI 不作依据）

| 门禁 | 结果 |
| --- | --- |
| backend `gofmt -l .` / `go vet ./...` | 通过（无差异/无告警） |
| backend `go test ./...`（全量，test 库 + Redis DB15 隔离） | 104 包全 ok，0 FAIL |
| `go test ./internal/securitytests/...`（含 permmatrix） | 通过 |
| `pnpm check:ui-copy --strict` | 通过（无混排/内部码直出） |
| `pnpm test:frontend` | 375 passed |
| `pnpm test:contracts` | 17 passed |
| `pnpm test:collector` | 18 passed |
| `pnpm build:admin` / `pnpm build:collector` | 通过 |
| `pnpm test:e2e`（Playwright 全量） | 364 passed / 3 skipped，0 failed（37 分钟） |

## 3. Docker 全栈场景矩阵实测（`docker-compose.full.yml`，本轮分支构建镜像）

三部分共 **60/60 断言 PASS**（47 主矩阵 + 7 双租户隔离 + 6 限额 UI 联动）：

### 3.1 主链路与角色（47 项）

- **主链路**：采集任务/商品草稿列表 → DEMO-AT-1004 pending→paid 触发自动化（有轨迹）→ 采购单 generate → draft→pending_confirm→placing→placed →（MCP）paid→shipped → mark-delivered 入库 → 销售订单创建发货运单。全链状态机合法推进。
- **MCP 写五类动作全管道**：`orders_add_tag` / `orders_remove_tag` / `exceptions_mark`（handle+unmark）/ `procurement_mark_placed` / `procurement_fill_logistics` / `procurement_mark_paid` 全部 dry_run→确认 token→execute 实测通过；无 token execute 拒绝、换工具/参数漂移拒绝、重放返回 `alreadyExecuted=true` 且不重复变更。
- **mark-paid 三前提四拒绝**：未配上限 fail-closed 拒绝；配置后 dry_run 回显金额/币种/单笔与日累计上限/当日已用/采购单明细；execute placed→paid；四拒绝路径实测——金额差 0.01 拒、币种不符拒、超单笔上限拒、超日累计上限拒（被拒采购单状态确认未变）。
- **闸门**：环境闸门 `MCP_WRITE_ENABLED` + 租户开关 `mcp/write_enabled`（settings 仅 admin 可改，operator 40305）+ `write:ops` scope（operator 创建写 token 403；readonly token 看不到任何写工具；write token 暴露全部 6 写工具）。
- **审计权限收紧（R184/#369 面）**：admin 可见带 `mode` 写审计行；operator/readonly 审计列表 200 但写行全过滤，显式 `mode=execute` 筛选亦不泄漏。
- **权限矩阵**：view-only 店铺写 40303、未授权店铺写 404（无存在性泄露）、不存在店铺 404、readonly 写 40301。
- 说明：连续快速 dry_run 触发 42901 限流为预期治理行为（每 token 5 rps/burst 10），矩阵脚本按限速节流后全绿。

### 3.2 双租户隔离（7 项）

- 注册第二租户（邮箱验证码链路）；租户 B 看不到租户 A 订单/采购单；租户 B 未开写开关 dry_run 被租户闸门拒绝（写 token 已持有仍拒，三层闸门独立生效）；租户 A 写 token 访问租户 B 订单/任意不存在采购单均 404 语义拒绝；租户 B 审计不含租户 A 执行行。
- **零残留**：矩阵后执行 `seed:demo:full:clean` + `verify`——**全部计数为 0，zero DEMO- residual rows**（临时 view-only 账号、R186 token、mcp 限额/开关 settings 均已回收）。

### 3.3 #372 新限额 UI 表单联动实测（6 项）

`/settings/mcp-tokens` mark-paid 金额限额表单（#372 新增，admin-only）：表单渲染 → 填 888.88/8888.88 保存出「已保存 mark-paid 金额限额」toast → 后端 `GET /api/v1/settings` 确认 `mcp/mark_paid_single_limit`、`mcp/mark_paid_daily_limit` 持久化 → 刷新后表单回显后端值 → 无 console/page 错误。UI 表单与后端 W3 上限校验为同一 settings 键，联动闭环。

## 4. P0/P1 修复与 P2 清单

- **P0/P1：0 项**，无需修复（矩阵调试期的失败均为测试脚本口径问题——响应字段名、采购单状态机命名、限流节流，非产品缺陷）。
- **P2 新增：无。** 存量 P2 保持：前端工具链跨 major 升级待决策（`docs/DEPENDENCY_AUDIT_R184.md`）；非 PostgreSQL 部署 advisory lock 软保证口径（R184 已登记）。

## 5. 合并顺序结论

1. **#371**（R185 线1，docs/脚本警示）→ 2. **#372**（R185 线2，UX v13 含限额 UI 表单）→ 3. **本 R186 集成 PR**。本分支已按此顺序叠加并全量回归，前两者按序合入后本 PR 无冲突收敛。
