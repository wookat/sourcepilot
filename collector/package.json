{
  "name": "@trademind/collector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "collect:test": "tsx src/scripts/collect-test.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "install:browsers": "playwright install chromium",
    "test": "vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "playwright": "^1.62.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  }
}
